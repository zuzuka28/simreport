apiVersion: v1
kind: Service
metadata:
  name: {{ include "anysave.fullname" . }}
  labels:
    {{- include "anysave.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anysave.selectorLabels" . | nindent 4 }}
